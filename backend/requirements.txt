# Backend Dependencies for AdmitScope
# Pinned for Python 3.11 compatibility on Render

# Core Data Science Libraries
numpy==1.26.4
pandas==2.2.2
scikit-learn==1.4.2
xgboost==2.0.3
shap==0.45.1

# Deep Learning Framework (CPU-only — no GPU needed on Render, much smaller image)
tensorflow-cpu==2.15.1

# Web Framework (FastAPI)
fastapi==0.110.3
uvicorn==0.29.0
python-multipart==0.0.9
pydantic==2.7.1

# Utilities
python-dotenv==1.0.1
